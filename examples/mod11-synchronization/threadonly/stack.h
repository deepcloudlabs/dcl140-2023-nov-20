/*******************************************************************************
This module contains declarations and definitions for module stack.c
/******************************************************************************/

/* Push a character onto the stack. */
void push(char);

/* Pop a character off the stack. */
char pop();
