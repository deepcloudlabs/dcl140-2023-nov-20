/*******************************************************************************
This module contains declarations and definitions for module proc_stack.c
/******************************************************************************/

/* Initialize the stack. */
void stackInit();

/* Push a character onto the stack. */
void push(char);

/* Pop a character off the stack. */
char pop();
